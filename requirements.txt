# Cross-Platform Core Dependencies
pandas>=2.0.0
seaborn>=0.13.0
matplotlib>=3.8.0
psutil>=5.9.0
APScheduler>=3.10.0
pymongo>=4.6.0
requests>=2.31.0
pillow>=10.0.0

# Raspberry Pi Hardware Dependencies ONLY (Skipped on macOS)
spidev; sys_platform == 'linux'
lgpio; sys_platform == 'linux'
rpi-lgpio; sys_platform == 'linux'
gpiozero; sys_platform == 'linux'